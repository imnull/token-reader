{
  "name": "token-reader",
  "version": "1.0.0",
  "description": "token reader compiler",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest",
    "build": "rm -rf ./dist && tsc ./src/index.ts --target es5 --declaration --outDir ./dist"
  },
  "keywords": [
    "token",
    "reader",
    "compiler"
  ],
  "author": "imnull@outlook.com",
  "license": "ISC",
  "devDependencies": {
    "jest": "^26.6.3"
  }
}
